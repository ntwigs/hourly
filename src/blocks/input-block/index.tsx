import { Section } from '../../components/section'

interface Props {
  title: string
}

export const InputBlock = ({ title }: Props): JSX.Element => {
  return (
	<Section>
		<Title>
	</Section>
	)
}
